[project]
name = "cv-generator"
version = "1.5.0"
description = "CV generator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=0.5.23",
    "docling>=2.70.0",
    "faiss-cpu>=1.9.0.post1",
    "langchain>=1.3.14",
    "langchain-community>=0.3.10",
    "langchain-core>=0.3.15",
    "langchain-docling>=2.0.0",
    "langchain-ollama>=0.2.1",
    "langchain-openai>=0.2.12",
    "langflow>=1.10.0",
    "langgraph>=0.3.25",
    "markdown2>=2.5.5",
    "pypdf>=6.14.2",
    "python-docx>=1.1.2",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0.2",
    "transformers>=4.47.0",
    "weasyprint>=69.0",
    "langchain-google-genai",
]

[tool.uv]
package = true

[project.scripts]
kb-ingest = "kb_ingest:main"
cv-gen = "generate_cv:main"
doc-gen = "generate_document_cli:main"
ats-audit = "tools.kb_external_audit:main"
kb-cleanup = "kb_cleanup:main"

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pytest>=9.0.3",
    "pytest-cov>=6.0.0",
]

[tool.coverage.run]
relative_files = true
source = ["src"]
omit = [
    "src/generate_cv.py",
    "src/generate_document_cli.py",
    "src/kb_cleanup.py",
    "src/kb_ingest.py",
    "src/kb_skills_sync.py",
    "src/tools/**"
]
[tool.mypy]
python_version = "3.12"
mypy_path = "src"
explicit_package_bases = true
ignore_missing_imports = true
check_untyped_defs = true
